'use client';import {useEffect,useState} from 'react';export default function Holidays(){const [rows,setRows]=useState<any[]>([]),[date,setDate]=useState(''),[name,setName]=useState('');async function load(){setRows((await (await fetch('/api/holidays')).json()).data||[])}useEffect(()=>{load()},[]);async function add(){await fetch('/api/holidays',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({holiday_date:date,name})});setDate('');setName('');load()}return <div className="container"><h1>Holidays</h1><div className="card inline"><input aria-label="Date" className="input" type="date" value={date} onChange={e=>setDate(e.target.value)}/><input aria-label="Holiday name" className="input" placeholder="Holiday name" value={name} onChange={e=>setName(e.target.value)}/><button className="btn primary" disabled={!date||!name.trim()} onClick={add}>Add</button></div><div className="card"><table><thead><tr><th>Date</th><th>Name</th><th/></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.holiday_date}</td><td>{x.name}</td><td><button className="btn small danger" onClick={async()=>{await fetch('/api/holidays?id='+x.id,{method:'DELETE'});load()}}>Delete</button></td></tr>)}</tbody></table></div></div>}
