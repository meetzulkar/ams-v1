'use client';import {useEffect,useState} from 'react';export default function Audit(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{fetch('/api/audit').then(r=>r.json()).then(j=>setRows(j.data||[]))},[]);return <div className="container"><h1>Audit log</h1><div className="card tableWrap"><table><thead><tr><th>Time</th><th>Action</th><th>Entity</th><th>Details</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{new Date(x.created_at).toLocaleString('en-IN',{timeZone:'Asia/Kolkata'})}</td><td>{x.action}</td><td>{x.entity||'—'}</td><td><code>{JSON.stringify(x.details)}</code></td></tr>)}</tbody></table></div></div>}
